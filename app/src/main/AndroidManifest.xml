<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_shopping_list"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_shopping_list"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList"
        tools:targetApi="31" >

        <activity
            android:name=".activities.SplashActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:screenOrientation="portrait"
            android:name=".activities.MainActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.CreationListActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.NewProductActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.CreditsActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".activities.ListDetailActivity" />
    </application>

</manifest>